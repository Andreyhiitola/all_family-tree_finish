#!/bin/bash

# Простой скрипт запуска для локальной разработки

PORT=8760
PROJECT_DIR="all_family-tree_finish"

# Убиваем процесс на порту если занят
lsof -ti:$PORT | xargs kill -9 2>/dev/null

# Переходим в папку проекта
cd ~/Desktop/$PROJECT_DIR || exit 1

echo ""
echo "🌳 Family Tree Server"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "🌐 Откройте в браузере:"
echo "   http://localhost:$PORT"
echo ""
echo "🛑 Для остановки: Ctrl+C"
echo ""

# Запускаем сервер ИЗ ПАПКИ ПРОЕКТА
python3 -m http.server $PORT
